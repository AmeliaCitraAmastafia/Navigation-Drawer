<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.NavDrawerApp" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Set warna background window agar sama dengan BackgroundScreen di Compose -->
        <item name="android:windowBackground">@color/background_screen</item>
        <!-- Pastikan status bar tidak berubah warna saat transisi -->
        <item name="android:statusBarColor">@color/primary_dark</item>
    </style>
</resources>
